api:
  security:
    token:
      secret: private.key
      public: public.key
spring:
  application:
    name: StockFlow
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: true
server:
  port: 8080